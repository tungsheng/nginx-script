server {
       listen 80 default_server;
       listen [::]:80 default_server ipv6only=on;

       server_name tonka.sh www.tonka.sh;

       root /var/www/tonka.sh;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}
